/**
 * Migration: Add auto-connection fields to CONNECTIONS table
 * Epic 6, Story 6.2: Auto-Connection Engine
 * 
 * Adds fields to support AI-generated connection suggestions:
 * - confidence: How confident we are in this connection (0.0 - 1.0)
 * - reason: Why the connection was suggested
 * - isAutoGenerated: Whether this was created by AI or manually
 */

'use strict';

module.exports = {
  async up(queryInterface, Sequelize) {
    // Add confidence score column
    await queryInterface.addColumn('CONNECTIONS', 'confidence', {
      type: Sequelize.FLOAT,
      allowNull: true,
      defaultValue: null,
      comment: 'Confidence score for auto-generated connections (0.0 - 1.0)',
    });

    // Add reason column
    await queryInterface.addColumn('CONNECTIONS', 'reason', {
      type: Sequelize.TEXT,
      allowNull: true,
      defaultValue: null,
      comment: 'Explanation of why the connection was suggested',
    });

    // Add isAutoGenerated flag
    await queryInterface.addColumn('CONNECTIONS', 'isAutoGenerated', {
      type: Sequelize.BOOLEAN,
      allowNull: false,
      defaultValue: false,
      comment: 'Whether this connection was auto-generated by AI',
    });
  },

  async down(queryInterface, Sequelize) {
    await queryInterface.removeColumn('CONNECTIONS', 'confidence');
    await queryInterface.removeColumn('CONNECTIONS', 'reason');
    await queryInterface.removeColumn('CONNECTIONS', 'isAutoGenerated');
  },
};
